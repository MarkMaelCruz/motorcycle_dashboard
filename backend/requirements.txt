flask
flask-cors
gunicorn

# JOB B additive dependencies for riding-state classification.
# MERGE these lines into your existing backend/requirements.txt.
# Do NOT delete your existing entries (Flask, flask-cors, gunicorn, etc.)
# � this is an addition, not a replacement.
 
joblib>=1.3
scikit-learn>=1.3
lightgbm>=4.1
numpy>=1.26
 